---
name: Feature
about: One independently reviewable unit of work
title: ''
labels: ''
assignees: ''
---

## Goal

<!-- What outcome should this Issue achieve? One or two sentences, stated as a
     result, not as a task list. -->

## Context

<!-- Why is this required now? What depends on it? Link the Issues this builds
     on and the Issues that are blocked by it. -->

## Depends on

<!-- Issues that must be CLOSED before this one can start. The autonomous loop
     reads this block, so keep the format:

       - #12
       - #13

     Write `none` when nothing blocks it — that is a statement, not an omission,
     and it is what tells the loop this Issue is ready to run. -->

none

## Acceptance Criteria

<!-- Observable, checkable statements. Each one should be something a reviewer
     can verify without reading the implementer's mind. Prefer behaviour over
     implementation. -->

- [ ]
- [ ]
- [ ]

## Technical Notes

<!-- Constraints and existing architecture the implementer must respect: which
     layer the change belongs in, which package owns it, invariants that must
     hold. Link to docs/architecture.md rather than restating it.

     Do not over-specify implementation. State the constraints; leave the
     approach to whoever picks this up. -->

## Out of Scope

<!-- Adjacent work that must NOT be done here, with the Issue that covers it
     where one exists. This section is what keeps a change reviewable. -->

-
-

## Verification

<!-- What should be run or observed to believe this works: commands, the tests
     that must exist, manual checks against the running stack. -->

-
-
